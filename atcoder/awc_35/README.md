## trick 记录

1. 给定两个序列 $A$, $B$, 长度分别为 $n, m$, 如果 $A_i \ge B_j$ 则 $i, j$ 配对成功，序列里面的每个数只能唯一匹配一次，求最大匹配数
  - 做法一：经典最大匹配贪心，两个序列排序后，对于 $B_i$ , 找到最小的满足 $A_i\ge B_j$ 的 $i$ 匹配
  - 做法二：直接二分答案 $k$, 贪心肯定是选序列 $A$ 最大的 $k$ 个 和 序列 $B$ 最小的 $k$ 个做匹配

    $A_{n-k+1}, A_{n-k},...,A_{n}$

    $B_1, B_2,...,B_k$ 
